{
  "name": "bootcamp-social-api",
  "version": "1.0.0",
  "description": "Simple backend server that uses a CLI to create user accounts, post messages, and delete them.",
  "main": "index.js",
  "scripts": {
    "seed": "node seeds/seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-fns": "^2.30.0",
    "express": "^4.18.2",
    "mongoose": "^7.6.3"
  }
}
